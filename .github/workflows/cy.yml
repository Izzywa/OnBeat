name: Test
on: 
    push:
        branches:
            - main

jobs: 
    test_app:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Run Unit test
              run: |
                    apt install python3-Django
                    cd OnBeat
                    python3 manage.py test
